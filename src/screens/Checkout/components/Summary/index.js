import "./style.scss";

function Summary({formData = {}, setFormData = () => {}}) {
    return(
        <section className="component-summary">
            Summary
        </section>
    );
}

export default Summary;